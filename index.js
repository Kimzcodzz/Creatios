import Link from 'next/link'; import { useLocale } from '../src/contexts/LocaleContext'; import { useTheme } from '../src/contexts/ThemeContext';
export default function Home(){ const {t}=useLocale(); const {skin}=useTheme(); return (<div className="p-6"><div className="flex justify-between items-center mb-4"><h1 className="text-2xl font-bold">{t('dashboard_title')||'Unified Intake Dashboard'}</h1><nav className="space-x-4"><Link className="text-blue-600 underline" href="/settings">Settings</Link><Link className="text-blue-600 underline" href="/login">Login</Link></nav></div><p>Active Theme: <b>{skin?.name}</b></p></div>);}